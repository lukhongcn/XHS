namespace LabelHelp.Enums
{
    /// <summary>
    /// 标签模板类型。
    /// 后续新增标签格式时，在这里扩展枚举。
    /// </summary>
    public enum LabelTemplateType
    {
        TableLabel,          // 表格型零件标签
        CardSingleLabel,     // 单件标签
        CardPackageLabel     // 最小包装标签
    }
}
