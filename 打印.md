# PrintClientWorker 标签打印定位

## 结论

`CheryCheckSystem.PrintClient.PrintClientWorker` 中，具体打印标签的位置在：

- [CheryCheckSystem.WinForms/PrintClientWorker.cs](D:/project/XHS/CheryCheckSystem.WinForms/PrintClientWorker.cs):93

```csharp
printerOutput.PrintSingleLabel(
    record.labelInfo,
    LabelTemplateType.TableLabel,
    printConfig,
    printerName);
```

这段代码位于 `ProcessRecords(IEnumerable<PrintPendingRecord> records)` 方法中，表示每条待打印记录都会在这里被发送到标签打印流程。

## 调用链

完整调用链如下：

1. `PrintClientWorker.ProcessOnce()`
2. `PrintClientWorker.ProcessRecords(...)`
3. `LabelPrinterOutput.PrintSingleLabel(...)`
4. `PrintDocument.Print()`

对应位置：

- [CheryCheckSystem.WinForms/PrintClientWorker.cs](D:/project/XHS/CheryCheckSystem.WinForms/PrintClientWorker.cs):29
  `ProcessOnce()` 负责拉取待打印记录并进入处理流程。
- [CheryCheckSystem.WinForms/PrintClientWorker.cs](D:/project/XHS/CheryCheckSystem.WinForms/PrintClientWorker.cs):73
  `ProcessRecords(...)` 负责遍历每条记录。
- [CheryCheckSystem.WinForms/PrintClientWorker.cs](D:/project/XHS/CheryCheckSystem.WinForms/PrintClientWorker.cs):93
  `printerOutput.PrintSingleLabel(...)` 是 `PrintClientWorker` 中直接触发标签打印的调用点。
- [LabelHelp/Printer/LabelPrinterOutput.cs](D:/project/XHS/LabelHelp/Printer/LabelPrinterOutput.cs):66
  `document.Print();` 是最终下发到 Windows 打印机的地方。

## 底层打印实现

`PrintSingleLabel(...)` 的实现在：

- [LabelHelp/Printer/LabelPrinterOutput.cs](D:/project/XHS/LabelHelp/Printer/LabelPrinterOutput.cs):16

该方法的关键逻辑：

1. 创建 `PrintDocument`
2. 设置 `PrinterSettings.PrinterName`
3. 设置纸张、边距、横竖向
4. 在 `PrintPage` 事件中绘制标签内容
5. 调用 `document.Print()` 真正打印

其中标签内容绘制发生在：

- [LabelHelp/Printer/LabelPrinterOutput.cs](D:/project/XHS/LabelHelp/Printer/LabelPrinterOutput.cs):62

```csharp
new LabelRenderer().Render(graphics, info, templateType, renderConfig, renderOffsetX, renderOffsetY);
```

这表示标签的版面、文字、二维码等内容，是由 `LabelRenderer` 负责绘制到打印画布上的。

## 打印机选择

打印机名称是在这里解析的：

- [CheryCheckSystem.WinForms/PrintClientPrinterResolver.cs](D:/project/XHS/CheryCheckSystem.WinForms/PrintClientPrinterResolver.cs):8

解析顺序：

1. 优先使用配置中的 `_settings.PrinterName`
2. 如果没有配置，则尝试匹配包含 `DL-740C` 的标签打印机
3. 再不行就使用系统默认打印机

## 与 PDF 打印的区别

- [CheryCheckSystem.WinForms/PdfShellPrintService.cs](D:/project/XHS/CheryCheckSystem.WinForms/PdfShellPrintService.cs):10

`PdfShellPrintService` 是另一套“打印 PDF 文件”的实现，底层通过外部 PDF 阅读器或 Shell 执行打印。

但当前 `PrintClientWorker` 并没有调用这个类。

因此当前结论是：

- `PrintClientWorker` 走的是“直接渲染标签并打印”的流程
- 不是“先生成 PDF，再打印 PDF”的流程

## 如果还要继续往下查

如果需要继续追踪“标签内容具体怎么画出来”，下一步建议查看：

- [LabelHelp/Rendering/LabelRenderer.cs](D:/project/XHS/LabelHelp/Rendering/LabelRenderer.cs)

这个文件负责标签版面绘制，是标签字段布局和渲染的核心实现。

## 标签尺寸从 100x150 改成 100x85 的修改方法

当前 `PrintClientWorker` 使用的是卷纸配置：

- [LabelHelp/Config/LabelPrintConfig.cs](D:/project/XHS/LabelHelp/Config/LabelPrintConfig.cs):57

也就是 `LabelPrintConfig.LoadRollPaper()`。

实际尺寸不是写死在 `PrintClientWorker` 中，而是来自配置文件：

- [CheryCheckSystem.WinForms/App.config](D:/project/XHS/CheryCheckSystem.WinForms/App.config):44

原配置：

```xml
<add key="Label.Roll.Width" value="100" />
<add key="Label.Roll.Height" value="150" />
<add key="Label.Roll.PageWidth" value="100" />
<add key="Label.Roll.PageHeight" value="150" />
<add key="Label.Roll.PrintRenderWidth" value="96" />
<add key="Label.Roll.PrintRenderHeight" value="150" />
```

修改为：

```xml
<add key="Label.Roll.Width" value="100" />
<add key="Label.Roll.Height" value="85" />
<add key="Label.Roll.PageWidth" value="100" />
<add key="Label.Roll.PageHeight" value="85" />
<add key="Label.Roll.PrintRenderWidth" value="96" />
<add key="Label.Roll.PrintRenderHeight" value="85" />
```

修改时需要一起关注这 3 组值：

1. `Label.Roll.Height`
2. `Label.Roll.PageHeight`
3. `Label.Roll.PrintRenderHeight`

其中：

- `Label.Roll.Height` 表示标签高度
- `Label.Roll.PageHeight` 表示打印页高度
- `Label.Roll.PrintRenderHeight` 表示实际渲染高度

宽度仍然是 `100` 时，`Label.Roll.PrintRenderWidth` 可以先保持 `96`，表示左右保留少量边距。

## 改尺寸后的风险点

即使尺寸改成 `100x85`，也不代表版面一定自动适配。

因为标签内容是由下面这个位置绘制的：

- [LabelHelp/Printer/LabelPrinterOutput.cs](D:/project/XHS/LabelHelp/Printer/LabelPrinterOutput.cs):62

```csharp
new LabelRenderer().Render(graphics, info, templateType, renderConfig, renderOffsetX, renderOffsetY);
```

真正的版面逻辑在：

- [LabelHelp/Rendering/LabelRenderer.cs](D:/project/XHS/LabelHelp/Rendering/LabelRenderer.cs)

所以把高度从 `150` 改成 `85` 后，可能出现：

- 文字区域被压缩
- 二维码过大
- 底部内容被截断
- 行距不合适

如果实际打印效果异常，需要继续调整：

1. `Label.TitleFontSize`
2. `Label.ValueFontSize`
3. `Label.Roll.PrintOffsetX`
4. `Label.Roll.PrintOffsetY`
5. `LabelRenderer.cs` 中的字段布局和绘制坐标

## 标签尺寸从 100x150 改成 100x85 的修改方法

当前 `PrintClientWorker` 使用的是卷纸配置：

- [LabelHelp/Config/LabelPrintConfig.cs](D:/project/XHS/LabelHelp/Config/LabelPrintConfig.cs):57

也就是 `LabelPrintConfig.LoadRollPaper()`。

实际尺寸不是写死在 `PrintClientWorker` 中，而是来自配置文件：

- [CheryCheckSystem.WinForms/App.config](D:/project/XHS/CheryCheckSystem.WinForms/App.config):44

原配置：

```xml
<add key="Label.Roll.Width" value="100" />
<add key="Label.Roll.Height" value="150" />
<add key="Label.Roll.PageWidth" value="100" />
<add key="Label.Roll.PageHeight" value="150" />
<add key="Label.Roll.PrintRenderWidth" value="96" />
<add key="Label.Roll.PrintRenderHeight" value="150" />
```

修改为：

```xml
<add key="Label.Roll.Width" value="100" />
<add key="Label.Roll.Height" value="85" />
<add key="Label.Roll.PageWidth" value="100" />
<add key="Label.Roll.PageHeight" value="85" />
<add key="Label.Roll.PrintRenderWidth" value="96" />
<add key="Label.Roll.PrintRenderHeight" value="85" />
```

修改时需要一起关注这 3 组值：

1. `Label.Roll.Height`
2. `Label.Roll.PageHeight`
3. `Label.Roll.PrintRenderHeight`

其中：

- `Label.Roll.Height` 表示标签高度
- `Label.Roll.PageHeight` 表示打印页高度
- `Label.Roll.PrintRenderHeight` 表示实际渲染高度

宽度仍然是 `100` 时，`Label.Roll.PrintRenderWidth` 可以先保持 `96`，表示左右保留少量边距。

## 改尺寸后的风险点

即使尺寸改成 `100x85`，也不代表版面一定自动适配。

因为标签内容是由下面这个位置绘制的：

- [LabelHelp/Printer/LabelPrinterOutput.cs](D:/project/XHS/LabelHelp/Printer/LabelPrinterOutput.cs):62

```csharp
new LabelRenderer().Render(graphics, info, templateType, renderConfig, renderOffsetX, renderOffsetY);
```

真正的版面逻辑在：

- [LabelHelp/Rendering/LabelRenderer.cs](D:/project/XHS/LabelHelp/Rendering/LabelRenderer.cs)

所以把高度从 `150` 改成 `85` 后，可能出现：

- 文字区域被压缩
- 二维码过大
- 底部内容被截断
- 行距不合适

如果实际打印效果异常，需要继续调整：

1. `Label.TitleFontSize`
2. `Label.ValueFontSize`
3. `Label.Roll.PrintOffsetX`
4. `Label.Roll.PrintOffsetY`
5. `LabelRenderer.cs` 中的字段布局和绘制坐标
